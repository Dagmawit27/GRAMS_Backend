spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ethio_rental_db}
spring.datasource.username=${DB_USERNAME:name}
spring.datasource.password=${DB_PASSWORD:password}
