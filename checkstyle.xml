<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    GRAMS Checkstyle Ruleset
    Based on Google Java Style Guide, relaxed for practicality.
    - No Javadoc requirement (not enforced everywhere)
    - Fails on: unused imports, line length > 120, missing braces on control statements
    - Severity threshold: HIGH only (see pom.xml for enforcement)
-->
<module name="Checker">
    <property name="severity" value="warning"/>
    <property name="charset" value="UTF-8"/>

    <!-- Line length: 120 characters -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package.*|^import.*|a]href|href|http://|https://|ftp://"/>
    </module>

    <module name="TreeWalker">
        <!-- ===== Imports ===== -->
        <!-- Fail on unused imports -->
        <module name="UnusedImports"/>
        <!-- Fail on star imports (e.g. import java.util.*) -->
        <module name="AvoidStarImport"/>

        <!-- ===== Naming ===== -->
        <!-- Google-style naming conventions -->
        <module name="TypeName"/>
        <module name="MemberName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="PackageName"/>
        <module name="ConstantName"/>

        <!-- ===== Braces ===== -->
        <!-- Fail on missing braces for control statements (if/else/for/while/do) -->
        <module name="NeedBraces"/>
        <!-- Left brace on same line (K&R style) -->
        <module name="LeftCurly"/>
        <!-- Right brace policy -->
        <module name="RightCurly"/>

        <!-- ===== Whitespace ===== -->
        <module name="WhitespaceAround">
            <property name="allowEmptyConstructors" value="true"/>
            <property name="allowEmptyMethods" value="true"/>
            <property name="allowEmptyTypes" value="true"/>
            <property name="allowEmptyLoops" value="true"/>
        </module>
        <module name="WhitespaceAfter"/>
        <module name="GenericWhitespace"/>

        <!-- ===== Coding ===== -->
        <!-- Require return at end of non-void methods (Google style) -->
        <!-- Removed: TooManyMethods, JavadocMethod, etc. for pragmatism -->

        <!-- ===== Modifiers ===== -->
        <!-- Redundant modifiers (e.g. public on interface methods) -->
        <module name="RedundantModifier"/>

        <!-- ===== Indentation ===== -->
        <!-- Google-style 4-space indentation -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="8"/>
            <property name="lineWrappingIndentation" value="8"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!-- ===== Annotations ===== -->
        <module name="AnnotationLocation">
            <property name="allowSamelineMultipleAnnotations" value="false"/>
            <property name="allowSamelineSingleParameterlessAnnotation" value="true"/>
            <property name="allowSamelineParameterizedAnnotation" value="false"/>
        </module>
    </module>
</module>
